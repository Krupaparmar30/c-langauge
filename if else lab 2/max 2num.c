#include<stdio.h>

main()
{
//   	1; WAP to Find max< from given 2 no< u2ing ternary operator.
     int a,b;
     printf("enter the values:");
     scanf("%d%d",&a,&b);
     
     (a>b)?printf("a is maximum"):printf("b is maximum");
}